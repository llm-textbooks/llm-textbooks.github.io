apiVersion: v1
kind: Service
metadata:
  name: agent-api
  labels:
    app.kubernetes.io/name: agent-api
spec:
  selector:
    app.kubernetes.io/name: agent-api
  ports:
    - name: http
      port: 8080
      targetPort: http
